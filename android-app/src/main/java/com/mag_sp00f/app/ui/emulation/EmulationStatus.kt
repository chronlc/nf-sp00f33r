package com.mag_sp00f.app.ui.emulation

/**
 * Emulation Status Enum
 * 
 * Following naming_scheme.md conventions for enums
 */
enum class EmulationStatus {
    STOPPED,
    RUNNING,
    PAUSED,
    ERROR
}